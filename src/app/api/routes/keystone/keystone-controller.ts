import { Logger, LoggerFactory, RestController } from '../../../common';

export class KeystoneController extends RestController {
  constructor() {
    super();
  }

  private static readonly LOGGER: Logger = LoggerFactory.getLogger();

  }
}
